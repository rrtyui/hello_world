#include <stdio.h>
#include <stdio.h>
/**
 * main - this program prints the followin text
 * Return: zero
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		printf("\n");
		i++;
	}
	return (0);
}
