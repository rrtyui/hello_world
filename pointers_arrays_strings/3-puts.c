#include "main.h"
/**
* _strlen - return the length of a string
* @s: parameterer
* Return: l
*/

void _puts(char *str)
{
	int i = 0;


	while (str[i] != '\0')
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
