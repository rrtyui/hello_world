#include "main.h"
/**
* puts_half - return the length of a string
* @str: parameterer
*/

char *_strcat(char *dest, char *src)
{
	int l;

	while (dest[l] != '\0')
	{
		l++;
	}

	char *pt = dest + l;

	while (src != '\0')
	{
		ptr = '\0';
	}

	return (dest);
}
