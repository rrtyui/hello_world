#include "main.h"
/**
* *_strcpy - return the length of a string
* @dest: parameterer
* @src: parametrer
*/

char *_strcpy(char *dest,char *src)
{
	int index = 0;

	while (src[i])
	{
		dest[i] = src[i];
		i++;
	}

	return (dest);
}
