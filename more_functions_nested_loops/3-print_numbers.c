#include "main.h"
#include <stdio.h>

/**
* _isupper - checks for upper case
*@c: variable
* Return: 1 if its upper and cero otherwise
*/

void print_numbers(void)
{
	char n;

	for (n >= 48; n <= 58 ; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
