#include "main.h"
/**
*_abs - do the abs of the enter value
*Return: 1 if is lower or 0 otherwise
*@c: is the parametrer given to the funct
*/

int _abs(int)
{
	if (n > 0)
	{
		return(n);
	}
	return (-n);
}
